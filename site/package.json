{
    "name": "velojs-site",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build && cd dist && zip -r ../site.zip . && cd .. && echo 'site.zip ready'"
    },
    "dependencies": {
        "@preact/signals": "^2.8.0",
        "preact": "^10.29.0"
    },
    "devDependencies": {
        "@preact/preset-vite": "^2.10.5",
        "@vanilla-extract/css": "^1.20.1",
        "@vanilla-extract/vite-plugin": "^5.2.2",
        "archiver": "^7.0.1",
        "marked": "^15.0.0",
        "shiki": "^3.0.0",
        "typescript": "^6.0.2",
        "vite": "^7.3.0"
    }
}
